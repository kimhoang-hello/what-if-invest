# Free API key from https://twelvedata.com — enables real historical price
# data (US exchanges + Toronto Stock Exchange). Without this, the app runs
# entirely on generated demo data.
TWELVE_DATA_API_KEY=
