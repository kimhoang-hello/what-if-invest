export function AppFooter() {
  return (
    <footer className="mt-4 border-t border-border pt-6 text-xs text-muted-foreground">
      What If Invest is an educational tool. Prices come from Twelve Data where available, with simulated data as a
      fallback for funds it doesn&apos;t cover — see the &quot;Simulated&quot; tag when that applies. Nothing here is
      investment advice. Past performance never guarantees future results — even when it&apos;s real.
    </footer>
  );
}
